# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Scraper Deployment

on:
  # push:
  #   branches:
  #     - master
  #     - integration
  #   paths:
  #     - geniespeaks/**
  workflow_call:
    inputs:
      app_name:
        description: App Name
        required: true
        type: string
      registry:
        description: Container Registry without https
        required: true
        type: string
      registry_uri:
        description: Container Registry with https
        required: true
        type: string
      environment:
        description: Environment for deployment
        required: true
        type: string

    secrets:
      azure_username:
        description: azure_username to deploy in ACR
        required: true
      
      azure_password:
        description: azure_password to deploy in ACR
        required: true

      azure_publish_profile:
        description: azure_publish_profile to deploy in ACR
        required: true
  workflow_dispatch:

jobs:
  pretend:
    name: Scraper Deployment
    run: echo "scraper Deploys here"
  # build:
  #   runs-on: 'ubuntu-latest'

  #   steps:
  #     - uses: actions/checkout@v2

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v1

  #     - name: Log in to registry
  #       uses: docker/login-action@v1
  #       with:
  #         registry: ${{ inputs.registry_uri }}
  #         username: ${{ secrets.azure_username }}
  #         password: ${{ secrets.azure_password }}

  #     - name: Build and push container image to registry
  #       uses: docker/build-push-action@v2
  #       with:
  #         push: true
  #         tags: ${{ inputs.registry }}/${{ secrets.azure_username }}/server:${{ github.sha }}
  #         file: ./server/server.dockerfile

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   environment:
  #     name: ${{ inputs.environment }}
  #     url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

  #   steps:
  #     - name: Deploy to Azure Web App
  #       id: deploy-to-webapp
  #       uses: azure/webapps-deploy@v2
  #       with:
  #         app-name: ${{ inputs.app_name }}
  #         slot-name: ${{ inputs.environment }}
  #         publish-profile: ${{ secrets.azure_publish_profile }}
  #         images: '${{ inputs.registry }}/${{ secrets.azure_username }}/server:${{ github.sha }}'
